//. . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . .

foo (
	int a = 1,
	int b = 2,
	int c = 3,
	int d = 4
	)
{
	printf ("foo (%d, %d, %d, %d)\n", a, b, c, d);
}

int 
main ()
{
	printf ("main ()\n");

	foo (, 200,, 400);

	thin function* pf (
		int x = 10,
		int y = 20,
		int z = 30,
		int w = 40  
		) = foo;

	pf (1000,, 3000);
	
	return 0;
}

//.............................................................................

int
printf (
	unsafe const char* pFormat,
	unsafe ...
	);

//.............................................................................
