//.............................................................................

foo (
	int x,
	int y
	)
{
	printf ("foo (%d, %d)\n", x, y);
}

function bar (
	int x,
	int y
	)
{
	printf ("bar (%d, %d)\n", x, y);
}

int 
main ()
{
	printf ("hui govno i muravei\n");
	
	multicast mc (int);
	mc += foo ~(10);
	mc += foo ~(20);
	mc += bar ~(30);
	mc (123);

	mc.Clear ();

	mc.Add (bar ~(40));
	mc.Add (foo ~(50));
	mc.Call (456);

	return 0;
}

//.............................................................................

int
printf (
	unsafe const char* pFormat,
	unsafe ...
	);

//.............................................................................