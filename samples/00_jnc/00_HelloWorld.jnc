// The simplest possible program in Jancy. Looks just like C/C++, doesn't it?

//.............................................................................

import "jnc_std.jncx"
import "jnc_std_String.jnc"

// entry point

int main ()
{
	printf ("hello world!\n");
	return 0;
}

//.............................................................................
