.. include:: def.rst
.. index:: pair: struct; String
.. _doxid-type_string:

struct String
=============

.. toctree::
	:hidden:

The internal implementation of the built-in string type. Fields ``m_p`` and
``m_length`` can be accessed directly. To get the contents of ``m_sz``, cast
a string to ``char const*``.


.. ref-code-block:: jancy
	:class: doxyrest-overview-code-block

	
	struct String {
		// fields
	
		char const* readonly :target:`m_p<doxid-struct_member_string_m_p>`;
		:ref:`size_t<doxid-typedef_size_t>` readonly :target:`m_length<doxid-struct_member_string_m_length>`;
	};
