foo ()
{
	for (int i = 0; i < 10; i++)
	{
		new int;
	}
}

//.............................................................................

int main ()
{
	jnc.createThread (foo);
	jnc.createThread (foo);
	jnc.createThread (foo);
	jnc.createThread (foo);
	jnc.createThread (foo);

	return 0;
}

//.............................................................................
