//.............................................................................

typedef function MyFunc (A* a);

class A
{
	foo ()
	{
		printf ("A.foo (%x)\n", this);

		MyFunc* f = bar;

		

		f (new A);
	}	

	bar ()
	{
		printf ("A.bar (%x)\n", this);
	}
}

//.............................................................................

int main ()
{
	A a;
	a.foo ();

	return 0;
}

//.............................................................................
