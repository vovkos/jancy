//.............................................................................

// entry point

int bindable g_state;

int g_a;

reactor g_reactor ()
{
	g_a = 
		g_state == 10 ? 100 : 
		g_state == 20 ? 200 : 300;
}

int main ()
{
	return 0;
}

//.............................................................................
