//.............................................................................

struct Point
{
	int m_x;
	int m_y;
}

struct Line
{
	Point m_a;
	Point m_b;
}

int main ()
{
	printf ("main ()\n");

	char const* p = jnc.format ("offset = %d\n", offsetof (Line.m_b.m_y));
	printf ("s = %s\n", p);

	return 0;
}

//.............................................................................
