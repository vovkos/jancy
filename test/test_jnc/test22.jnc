//..............................................................................

write (
	int recordCode,
	int foldFlags = 0
	)
{
	printf ("write (%d, %d)\n", recordCode, foldFlags);
}

write (
	int recordCode,
	int p,
	int size,
	int foldFlags = 0
	)
{
	printf ("write (%d, %d, %d, %d)\n", recordCode, p, size, foldFlags);
}

int main ()
{
	printf ("main ()\n");

	write (10);

	return 0;
}

//..............................................................................
