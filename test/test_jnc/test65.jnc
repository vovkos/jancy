char const* bar ()
{
	int i = 10;
	return $"bar $i";
}

int main ()
{
	printf ("main ()\n");

	char const* filterString = $"bar returned: '%1'" (bar ());

	printf ("filterString = %s\n", filterString);

	return 0;
}

//.............................................................................

