jnc.AutomatonResult automaton myScanner (jnc.Recognizer* recognizer)
{
	%% \s* ("foo" \d*) \s+ ("bar" \d*)? \s*
		printf ("detected foobar: '%s'\n", recognizer.m_lexeme.m_text);
		printf ("foo: '%s'\n", recognizer.m_subLexemeArray [0].m_text);
		printf ("bar: '%s'\n", recognizer.m_subLexemeArray [1].m_text);
		printf ("offset: %d\n", recognizer.m_currentOffset);
}

int main ()
{
	jnc.Recognizer recognizer (myScanner);

	recognizer.recognize (" foo123 bar567 ");
	return 0;

catch:
	printf ($"Error: $!\n");
	return -1;
}
