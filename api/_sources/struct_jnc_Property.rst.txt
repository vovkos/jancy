.. index:: pair: struct; jnc_Property
.. _doxid-structjnc___property:

struct jnc_Property
===================

.. toctree::
	:hidden:

Opaque structure used as a handle to Jancy property.

    Use functions from the `Property` group to access and manage the contents of this structure.

