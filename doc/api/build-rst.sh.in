#!/bin/bash

set -e

echo Copying ${DOC_NAME} RST files to ${RST_DIR}...

rm -rf ${RST_DIR}
mkdir -p ${RST_DIR}
cp ${CMAKE_CURRENT_LIST_DIR}/rst/*.* ${RST_DIR}

echo Building ${DOC_NAME} XML files at ${XML_DIR}...

"${DOXYGEN_EXE}" ${DOXYGEN_CMD_LINE}

echo Building ${DOC_NAME} RST files at ${RST_DIR}...

"${DOXYREST_EXE}" ${DOXYREST_CMD_LINE}
