@echo off

set PDF_DIR="${PDF_DIR}"
set PDF_DIR=%PDF_DIR:/=\%

if exist %PDF_DIR% (rd /S /Q %PDF_DIR% || exit)
mkdir %PDF_DIR% || exit

echo Building ${DOC_NAME} Latex files at ${TEX_DIR}...

"${SPHINX_BUILD_EXE}" ${SPHINX_TEX_CMD_LINE}

echo Building ${DOC_NAME} PDF at ${PDF_DIR}...

set PREV_DIR=%CD%
cd ${TEX_DIR}

"${PDFLATEX_EXE}" ${LATEX_CMD_LINE}
"${PDFLATEX_EXE}" ${LATEX_CMD_LINE}
	
cd %PREV_DIR%
