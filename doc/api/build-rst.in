${SHELL_FILE_HDR}

echo Building Jancy API XML files at ${XML_DIR}...

${DOXYGEN_EXE} ${CMAKE_CURRENT_BINARY_DIR}/Doxyfile

echo Building Jancy API RST files at ${RST_DIR}...

${DOXYREST_EXE} ${SHELL_LINE_CONT}
	${XML_DIR}/index.xml ${SHELL_LINE_CONT}
	-D g_hasNewLineAfterReturnType ${SHELL_LINE_CONT}
	-o ${RST_DIR}/global.rst ${SHELL_LINE_CONT}
	-F ${DOXYREST_FRAME_DIR} ${SHELL_LINE_CONT}
	-f c_global.rst.in
